
with "registry";
with "shared";

project Main is
   for Source_Dirs use ("main");

   for Object_Dir use "obj/main";
   for Exec_Dir use ".";
   for Main use ("run.adb");

   package Compiler renames Shared.Compiler;
end Main;
