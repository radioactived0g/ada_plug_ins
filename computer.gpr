
with "registry";
with "shared";

library project Computer is

   for Source_Dirs use ("computer");

   for Object_Dir use "obj/computer";
   for Library_ALI_Dir use "lib/computer";
   for Library_Dir use "plugins";
   for Library_Name use Shared.Plugin_Prefix & "computer";
   for Library_Kind use "dynamic";
   for Library_Interface use ("computer");

   package Compiler renames Shared.Compiler;

end Computer;
